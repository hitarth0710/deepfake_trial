export const config = {
  apiUrl: "http://127.0.0.1:8000", // Django development server URL
  endpoints: {
    video: "/api/analyze/",
    audio: "/api/analyze-audio/",
    status: "/api/status/",
  },
};
